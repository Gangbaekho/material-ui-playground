import React from 'react'
import Navigation from './Navigation'

const Login = (props) =>{
    return (
        <div>
        <Navigation {...props}/>
            Login component
        </div>
    )
}

export default Login